1. На GitHub в репозитории нажать кнопку "Code" и скопировать SSH URL. В терминале с помощью команды git clone URL загрузить репозиторий.

2. В терминале выполнить команду git pull URL (origin)

3. git status предоставляет информацию о  изменении файлов
 git log выводит список коммитов с их хэшами, авторами, датами и сообщениями коммитов, что помогает отслеживать историю изменений в репозитории. 
(Для выхода использовать клавишу "q".)
